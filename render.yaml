services:
  - type: worker
    name: netflix-bulk-checker
    env: docker
    repo: https://github.com/YOUR_USERNAME/YOUR_REPO
    branch: main
    envVars:
      - key: BOT_TOKEN
        sync: false
        description: "Telegram Bot Token from @BotFather"
    healthCheckPath: /health
    autoDeploy: true
